#ifndef WORD_H
#define WORD_H

// Reads the next word from input, storing it in word.
// Makes word empty if no word could be read. Truncates
// if it exceeds len.

void read_word(char *word, int len);

#endif
